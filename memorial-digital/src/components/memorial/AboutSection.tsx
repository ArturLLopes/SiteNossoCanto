import { CalendarDays, MapPin } from 'lucide-react';
import { Card } from '@/components/ui/Card';
import { SectionTitle } from '@/components/ui/SectionTitle';
import { memorialInfo } from '@/data/memorial';
export function AboutSection() { return <section id="sobre" className="scroll-mt-24"><SectionTitle eyebrow="Sobre" title="Um pouco de sua história" description="Informações essenciais para apresentar a pessoa por trás das memórias."/><Card className="p-5 sm:p-7"><div className="grid gap-5 sm:grid-cols-2"><Info icon={<CalendarDays />} label="Nascimento" value={memorialInfo.birthDate}/><Info icon={<CalendarDays />} label="Falecimento" value={memorialInfo.deathDate}/><Info icon={<MapPin />} label="Nasceu em" value={memorialInfo.birthPlace}/><Info icon={<MapPin />} label="Partiu em" value={memorialInfo.deathPlace}/></div><div className="mt-7 border-t border-[var(--border)] pt-6"><p className="text-[15px] leading-7 text-[#555a53]">Eleanor trouxe calor para os ambientes, cuidava das pessoas com delicadeza e encontrava significado nos gestos cotidianos.</p></div></Card></section>; }
function Info({ icon,label,value }: { icon: React.ReactNode; label: string; value: string }) { return <div className="flex items-start gap-3"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-2xl bg-[#edf0e9] text-[var(--sage-dark)]">{icon}</span><div><p className="text-xs uppercase tracking-wider text-[var(--warm-gray)]">{label}</p><p className="mt-1 text-sm font-medium text-[#3f443e]">{value}</p></div></div>; }
