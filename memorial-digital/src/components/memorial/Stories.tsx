import { ArrowUpRight } from 'lucide-react';
import { Card } from '@/components/ui/Card';
import { SectionTitle } from '@/components/ui/SectionTitle';
import { stories } from '@/data/memorial';
export function Stories() { return <section id="historias" className="scroll-mt-24"><SectionTitle eyebrow="Histórias" title="Relatos de quem esteve presente" description="Cards reutilizáveis para registrar histórias pessoais e lembranças compartilhadas."/><div className="grid gap-4 md:grid-cols-3">{stories.map((story) => <Card key={story.id} className="p-5"><div className="text-xs text-[var(--warm-gray)]">{story.date}</div><h3 className="mt-2 font-display text-xl text-[#3c423b]">{story.title}</h3><p className="mt-2 text-sm leading-6 text-[#656a63]">{story.summary}</p><div className="mt-5 flex items-center justify-between border-t border-[var(--border)] pt-4"><span className="text-xs font-medium text-[#545a51]">Por {story.author}</span><button className="inline-flex min-h-10 items-center gap-1 text-sm font-semibold text-[var(--sage-dark)] focus:outline-none focus-visible:ring-2 focus-visible:ring-[var(--sage)]">Ler história <ArrowUpRight className="h-4 w-4"/></button></div></Card>)}</div></section>; }
