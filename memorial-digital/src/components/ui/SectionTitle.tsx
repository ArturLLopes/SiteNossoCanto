export function SectionTitle({ eyebrow, title, description }: { eyebrow?: string; title: string; description?: string }) { return <header className="mb-5"><div className="mb-2 text-xs font-semibold uppercase tracking-[0.2em] text-[var(--sage-dark)]">{eyebrow}</div><h2 className="font-display text-2xl font-medium text-[#343932] sm:text-3xl">{title}</h2>{description && <p className="mt-2 max-w-2xl text-sm leading-6 text-[var(--warm-gray)]">{description}</p>}</header>; }
